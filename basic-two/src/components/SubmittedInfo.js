import React from 'react';

const SubmittedInfo = (props) => {
    console.log(props)
    return (
        <div>
            <h2>Submit Page</h2>
        </div>
    );
};

export default SubmittedInfo;